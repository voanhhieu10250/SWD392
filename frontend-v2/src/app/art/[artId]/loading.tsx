import Spinner from "@/components/homepage/Spinner";
import React from "react";

const Loading = () => {
  return (
    <div className="mb-8">
      <Spinner />
    </div>
  );
};

export default Loading;
